# ============================================================
#  Sugar Corner ? Application Properties
# ============================================================

# ?? Server ??????????????????????????????????????????????????
server.port=${PORT:8080}

# ?? DataSource ??????????????????????????????????????????????
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=WQdEWZCaJRfZQJefygqiynMsamfBCucL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? Connection Pool (HikariCP) ???????????????????????????????
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# ?? Thymeleaf ????????????????????????????????????????????????
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

# ?? Multipart (profile photo upload) ?????????????????????????
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ?? Logging ??????????????????????????????????????????????????
logging.level.com.brownieshop=DEBUG

# ?? Session ??????????????????????????????????????????????????
server.servlet.session.timeout=30m
